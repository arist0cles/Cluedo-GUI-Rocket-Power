package core;

/**
 * The controller translates the user's interactions with the view into actions that the model will perform. 
 * In a stand-alone GUI client, user interactions could be button clicks or menu selections. Depending on the 
 * context, a controller may also select a new view -- for example, a web page of results -- to present back 
 * to the user.
 * 
 * @author Patrick and Kirita
 *
 */
public class Controller {

}
