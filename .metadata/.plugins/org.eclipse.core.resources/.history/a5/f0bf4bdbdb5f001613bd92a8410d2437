package ColorSchemes;

import java.awt.Color;

public class Emo extends ColorScheme{
	public Emo(){
		DOOR = new Color(0, 0, 0);
		HALLWAY = new Color(40, 40, 40);
		ROOM = new Color(60, 60, 60);
		WALL = new Color(80, 80, 80);
		GRID = new Color(0, 0, 0);
	}
}
